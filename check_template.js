const fs = require('fs');
const path = require('path');

// read template bytes and check some pixels
// well, we can't easily parse jpeg in pure node without a library.
// but we just need to know if the technique works.
