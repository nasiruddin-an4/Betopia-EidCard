const fs = require('fs');

// We need a way to just generate a test image offline without the browser to find the coords.
// Node-canvas would be great, but maybe we can just make an HTML file and open it locally.
